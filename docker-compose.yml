services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/nextjs-starter
      - NODE_ENV=production
      - NEXT_PUBLIC_APP_URL=http://localhost:3000
      - INTAKE_UPLOAD_DIR=/app/storage/intake
    volumes:
      - intake_uploads:/app/storage/intake
    depends_on:
      - mongodb
    networks:
      - app-network

  mongodb:
    image: mongo:7
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=nextjs-starter
    networks:
      - app-network

volumes:
  mongodb_data:
  intake_uploads:

networks:
  app-network:
    driver: bridge
